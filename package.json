{
  "name": "redstone-gallery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx netlify dev",
    "deploy": "npx netlify deploy --prod --dir=."
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.2",
    "@netlify/functions": "^3.1.10"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
